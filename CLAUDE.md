# 작업 규칙

이 폴더는 대학 강의 기말 **조별과제** "바이브코딩을 활용한 MVP 서비스 만들기" 작업 공간입니다.
서비스는 **「우리 뭐 먹지?」** — 여러 명이 각자 싫은 메뉴를 지워서 빠르게 메뉴를 정하는 웹서비스.
여러 대화 세션에 걸쳐 진행되므로 아래 규칙을 지켜 주세요.

## 세션 시작 시

- 작업을 시작하기 전에 **PROJECT_OVERVIEW.md를 먼저 읽고** 프로젝트 개요와 현재 진행 단계를 확인할 것
- 특히 **4절 "현재 상태"**가 이전 세션의 인수인계 내용이므로 반드시 확인할 것
- 서비스 기획의 세부 내용이 필요하면 **`기획안.md`**(팀원 제안 원안)를 볼 것

## 세션 끝날 때

- PROJECT_OVERVIEW.md **4절 "현재 상태"를 갱신**할 것 (마지막 업데이트 날짜, 현재 단계, 한 일, 다음 할 일, 막힌 것)

## 세션 진행 중

- 대화는 **한국어**로 진행
- 한 단계(7단계 중 하나)를 마치면 PROJECT_OVERVIEW.md의 **진행표 상태를 갱신**할 것
- 기능 범위, 기술 스택 등 TBD로 남아 있던 항목이 정해지면 PROJECT_OVERVIEW.md에 기록할 것
- **PROJECT_OVERVIEW.md 5절 "정해야 할 것"이 남아 있으면 코드를 쓰기 전에 먼저 결정할 것.**
  특히 기기 방식(한 기기 돌려쓰기 vs 각자 기기)은 구조 전체를 좌우한다

## 프로젝트 성격

- 상용 제품이 아니라 **MVP**를 만드는 것이 목표. 과도한 기능 추가나 조기 최적화는 지양
- 사용자는 각 단계를 직접 이해하며 진행하려는 학습 목적이 있으므로, 중요한 선택지가 있을 때는 이유를 짧게 설명할 것
- **조별과제다.** 코드와 문서를 팀원이 볼 수 있으므로, 결정의 근거를 문서에 남길 것

## 이 서비스에서 특히 신경 쓸 것

- **모바일이 주 사용 환경이다.** 여러 명이 핸드폰을 돌려가며 쓰는 상황을 전제로 만들 것.
  데스크톱은 그다음
- **핵심은 "AI 추천이 아니라 비선호 제거"다.** 이 주장을 흐리는 기능(AI 추천, 취향 학습 등)은
  넣지 말 것. 서비스의 논지 자체가 무너진다
- **KPI 측정이 이 프로젝트의 목적 중 하나다.** (PROJECT_OVERVIEW.md 9절)
  화면을 만들 때부터 "언제·무엇을 기록할지" 자리를 만들어 둘 것.
  최소한 **메뉴 결정에 걸린 시간**은 2단계부터 측정해야 한다 (결과 화면에 표시되므로)

## 저장소 경계 (중요)

- 이 폴더(`menu-veto/`)가 git 저장소 루트이고, **GitHub에 공개할 예정이다**
- 한 단계 위 `기말_프로젝트/`는 git 저장소가 아니다. 공개하면 안 되는 과제 파일(발표자료·보고서 등)은 그쪽에 둔다
- 커밋 전에 **공개돼도 괜찮은 파일인지 확인**할 것. 비밀값은 `.env`에 두고 절대 커밋하지 말 것
- 형제 폴더 `../skku-notice-hub/`는 **별개의 저장소**다. 참고는 하되 이쪽 커밋에 섞지 말 것

## 코드 작업 규칙

### 명령어

아직 앱을 만들기 전이라 비어 있습니다. **2단계에서 Next.js 프로젝트를 만들면 여기에 표를 채울 것.**
(이전 프로젝트에서는 `web/` 폴더에 앱을 두고 `npm run dev` / `build` / `lint`를 썼다)

### 화면(UI)을 고칠 때

- **`DESIGN.md`를 먼저 읽을 것.** 색·글자 크기·간격 규칙이 거기 정의된다
  - ⚠️ 아직 없다. **3단계에서 작성**한다. 그전까지는 색을 하드코딩하지 말고 최소한 한곳에 모아둘 것
- 색은 CSS 변수 토큰으로 쓸 것. `text-black/60` 같은 하드코딩 금지
- 라이트/다크 **양쪽 다** 확인할 것
- **모바일 화면을 먼저 확인할 것** (이 서비스의 주 사용 환경)

### 주석

- 코드 주석은 한국어로, **"무엇을"이 아니라 "왜"**를 적을 것 (사용자가 나중에 읽고 이해해야 함)

# DESIGN.md — 「우리 뭐 먹지?」

> 이 문서는 **AI에게 주는 디자인 규칙서**입니다.
> 화면을 새로 만들거나 고칠 때 이 문서를 먼저 읽고, 여기 정의된 토큰과 규칙만 사용하세요.
> 여기 없는 색·글자 크기·간격을 임의로 만들지 않습니다.

## 출처

강의에서 안내한 [awesome-design-md](https://github.com/voltagent/awesome-design-md)의
**Starbucks** 디자인 시스템을 바탕으로, 이 서비스에 맞게 다시 쓴 문서입니다.

**왜 Starbucks를 골랐나**

- **음식/음료 서비스**라 온도감이 맞는다
- **사진에 의존하지 않는다.** Airbnb·Pinterest는 사진이 시스템의 핵심이라
  사진 없는 우리가 가져오면 껍데기만 남는다. 우리에게는 이모지와 한글 텍스트뿐이다
- 크림색 캔버스·둥근 카드·알약형 버튼이 "여럿이 편하게 고르는" 분위기와 맞는다

**무엇을 가져오고 무엇을 바꿨나**

| 가져온 것 | 바꾼 것 |
|---|---|
| 크림 캔버스 위계 (캔버스 → 세라믹 → 흰 카드) | 초록 대신 **주황 4단계** |
| 모든 버튼 **알약형(full pill)** | 터치 타깃을 훨씬 크게 (아래 5절) |
| 카드 **12px 모서리 + 속삭이는 2겹 그림자** | 다크 모드 추가 (원본에는 없음) |
| **면 색을 바꿔** 특별한 구간 표시 | 그 구간을 **결과 화면**에 씀. 단, 라이트 모드에서는 어둡게가 아니라 **더 따뜻하게** |
| `scale(0.95)` 눌림 피드백 | 움직임 규칙을 대폭 추가 (7절) |
| 본문은 순수 검정을 쓰지 않음 | — |
| 자간을 타이트하게 (`-0.01em`) | — |

---

## 0. 이 서비스의 성격 (가장 중요)

여러 명이 **핸드폰 하나를 돌려가며** 쓰는 서비스입니다.
한 사람이 화면을 보는 시간은 몇 초뿐이고, 그동안 딱 한 가지 결정만 합니다.

디자인의 목표는 "정보를 많이 담는다"가 아니라
**"지금 뭘 해야 하는지 한눈에 알게 한다"**입니다.

- 화면당 요구하는 행동은 **하나**. 두 가지를 동시에 묻지 않는다
- 남의 폰을 급하게 누르는 상황이다. **타깃을 크게, 오탭 여지를 없앤다**
- 뭔가 일어났으면 **반드시 눈에 보이게** 알린다

### ⚠️ 이전 프로젝트(`skku-notice-hub`)의 DESIGN.md를 그대로 가져오지 말 것

저쪽은 공지를 **훑는** 도구라 "애니메이션 금지 / 그림자 금지"가 옳았습니다.
여기는 정반대입니다. 이 서비스에서 **움직임은 장식이 아니라 기능**입니다.

실제로 2단계에서 이 문제가 발생했습니다. 최종 선택 화면에서 아무 연출 없이
결과로 넘어가니 **"선택됐다는 걸 확인할 수가 없다"**는 지적이 나왔고,
룰렛 연출을 넣어서 해결했습니다.

---

## 1. 색

색 정의의 실체는 `web/src/app/globals.css`입니다. **이 문서의 표와 그 파일은 항상 같아야 합니다.**

### 브랜드 주황 4단계

Starbucks가 초록을 네 단계로 나눠 **역할마다 다른 초록**을 쓰듯,
우리도 주황을 네 단계로 나눕니다. **"주황 하나로 다 쓰기"를 하지 마세요.** 브랜드가 납작해집니다.

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--brand` | `#A8410D` | `#FF9A63` | **글자로 쓰는 주황.** 강조 낱말, "N번째 사람" |
| `--brand-accent` | `#C24E12` | `#FF8A4C` | **채우는 주황.** 주요 버튼 배경, 룰렛 하이라이트 |
| `--brand-strong` | `#93380A` | `#FFA771` | 눌림 상태 |
| `--brand-fg` | `#FFFFFF` | `#2A1006` | 채운 주황 위의 글자 |

> `--brand`와 `--brand-accent`를 나눈 이유: 크림 배경 위에서 글자로 읽히려면
> 더 어두워야 하고, 버튼을 채울 때는 좀 더 밝아야 눈에 들어옵니다.
> 하나로 합치면 둘 중 하나가 반드시 나빠집니다.

### 캔버스 3단계

**순백을 페이지 바탕으로 쓰지 않습니다.** 따뜻한 크림이 이 디자인의 뼈대입니다.
크림 위에 흰 카드가 놓여야 카드가 "떠 있는" 것으로 보입니다.

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--background` | `#F5F1EA` | `#171310` | 페이지 캔버스 (크림) |
| `--surface` | `#EDE8E0` | `#201A16` | 구역을 나누는 한 겹 진한 면 |
| `--card` | `#FFFFFF` | `#241D18` | 카드 표면 |

### 결과 존 (결과 화면)

Starbucks가 **면 색을 바꿔서** "여기는 특별한 구간"을 표시하듯,
우리도 **결과 화면 전체의 면 색을 바꿉니다.** 이 서비스의 클라이맥스이기 때문입니다.

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--house` | `#F9DEC9` | `#0D0907` | 결과 화면 배경 |
| `--house-fg` | `#3A1D0E` | `#F2EBE6` | 결과 존 위 글자 |
| `--house-muted` | `#7A5343` | `#A99C92` | 결과 존 위 보조 글자 |

> **라이트 모드에서는 어둡게 깔지 않습니다.** 처음엔 Starbucks의 진한 초록 밴드를
> 그대로 옮겨 결과 화면을 짙은 갈색으로 만들었는데, 밝은 화면 세 개를 지나오다
> 갑자기 어두워지니 **테마가 바뀐 것처럼 보여 흐름이 끊긴다**는 지적이 나왔습니다.
>
> 원본에도 밴드 말고 **크림 워시(`#faf6ee`)로 특별 구간을 표시하는 방식**이 따로 있습니다.
> 라이트 모드는 그쪽을 따라 **캔버스보다 더 따뜻한 살구빛**으로 갑니다.
>
> **규칙**: 특별한 구간이라는 신호는 "면 색이 바뀐다"로 충분하고,
> **바뀌는 방향은 각 모드의 밝기를 따라간다.** 라이트에서는 더 따뜻하게,
> 다크에서는 더 어둡게. 한쪽 모드의 색을 다른 쪽에 그대로 옮기지 마세요.

### 글자와 선

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--foreground` | `#241D18` | `#F2EBE6` | 제목·본문 |
| `--muted` | `#6B6259` | `#A99C92` | 설명·보조 문구 |
| `--border` | `#DDD5C9` | `#362D27` | 칩 테두리, 구분선 |
| `--card-border` | `#EBE4DA` | `#3B302A` | 카드 테두리 (그림자가 주역이라 아주 옅게) |

> **본문에 순수 검정을 쓰지 않습니다.** `#241D18`은 크림 캔버스의 온도에 맞춘 값입니다.
> 순수 검정은 따뜻한 배경 위에서 차갑게 튑니다.

### 탈락한 카드

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--card-out-bg` | `#EBE6DE` | `#1A1512` | 탈락 카드 바탕 |
| `--card-out-fg` | `#8A7F76` | `#7D726A` | 탈락 카드 글자 |
| `--card-out-border` | `#E2DAD0` | `#2A231F` | 탈락 카드 테두리 |

### 거부

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--danger` | `#C02626` | `#F87171` | 거부 버튼 글자 (일반 면 위) |
| `--danger-on-house` | `#B3251A` | `#FF9A9A` | 거부 버튼 글자 (결과 존 위) |

> 결과 존은 일반 면과 밝기가 달라서 같은 빨강을 올리면 탁해집니다.
> 그래서 존 전용 빨강을 따로 둡니다. **의미(거부=빨강)는 유지하고 명도만 맞추는** 방식입니다.

### 절대 규칙

- **순백을 페이지 캔버스로 쓰지 마세요.** 크림이 이 시스템의 서명입니다
- **주황을 하나로 통일하지 마세요.** 4단계는 의도된 것입니다
- **탈락은 회색, 살아있음은 흰 카드.** 이 대응을 다른 곳에 쓰지 마세요
- 색만으로 정보를 전달하지 않습니다. 탈락 카드에는 **취소선 + "탈락" 글자**를 같이 넣습니다
- 그라데이션을 쓰지 않습니다. 이 시스템은 **단색 블록**으로만 깊이를 만듭니다
- `text-black/60` 같은 하드코딩 금지. 항상 토큰을 씁니다

---

## 2. 글자

- 글꼴: 시스템 기본 (Geist → Pretendard → 맑은 고딕). **웹폰트를 새로 추가하지 않습니다**
  - Starbucks의 SoDoSans는 비공개 폰트라 쓸 수 없습니다. 원본 문서가 제안한 대체안
    (Inter/Manrope/Nunito Sans)도 **한글을 지원하지 않아** 우리에겐 의미가 없습니다
- **자간을 `-0.01em`로 타이트하게** 잡습니다. 전체가 조금 눌린 느낌이 이 디자인의 특징입니다
- 화면당 메시지가 하나뿐이므로 크게 잡습니다

| 이름 | 크기 / 굵기 | 용도 |
|---|---|---|
| 서비스명 | 36px, bold | 시작 화면의 "우리 뭐 먹지?" |
| 결과 메뉴명 | 36px, bold | 최종 선택된 메뉴 |
| 화면 질문 | 24px, bold, `leading-snug` | "절대 먹기 싫은 것 하나만 지워주세요" |
| 버튼 | 18px, bold | 주요 버튼 |
| 카드 메뉴명 | 16px, semibold | 메뉴 카드 안 |
| 본문 | 15px, normal | 설명 문구 |
| 보조 | 13px, normal, `--muted` | 안내·규칙 설명 |
| 카드 예시 | 11px, `--muted` | "짜장면 · 탕수육 · 마라탕" |

### 원칙

- **위계는 크기보다 굵기와 색으로 만듭니다.** Starbucks가 h1/h2를 같은 24px로 두고
  굵기(600 vs 400)와 색으로만 구분하는 방식을 따릅니다. 크기를 계속 키우면 금방 바닥납니다
- **질문은 줄바꿈을 직접 넣습니다.** 화면 너비에 따라 끊기는 자리가 달라지면
  급하게 읽을 때 의미 덩어리가 깨집니다
- 이모지는 크기로 위계를 만듭니다: 카드 30px, 전달 화면 48px, 결과 72px

---

## 3. 모서리

Starbucks의 규칙을 그대로 가져옵니다. **버튼과 카드의 모서리 성격을 다르게** 두는 게 핵심입니다.

| 값 | 어디에 |
|---|---|
| **9999px (완전한 알약)** | **모든 버튼, 예외 없음** |
| **12px** | 카드, 오버레이 안의 블록 |
| 9999px | 후보 칩 |

> **버튼 모서리를 각지게 만들지 마세요.** 알약형은 이 시스템의 서명입니다.
> 카드는 12px로 두어 버튼과 성격을 구분합니다. 둘 다 둥글면 위계가 사라집니다.

---

## 4. 깊이 (그림자)

**한 겹의 진한 그림자 대신, 낮은 투명도의 그림자를 2겹 쌓습니다.**
현실의 조명(주광 + 환경광)을 흉내 내는 방식이고, 이게 Starbucks 시스템의 핵심 감각입니다.

| 토큰 | 값 (라이트) | 어디에 |
|---|---|---|
| `--elev-card` | `0 0 .5px rgba(0,0,0,.14), 0 1px 1px rgba(0,0,0,.24)` | 살아 있는 메뉴 카드 |
| `--elev-lift` | `0 0 6px rgba(0,0,0,.24), 0 8px 12px rgba(0,0,0,.14)` | 룰렛 하이라이트 |

- 다크 모드에서는 그림자가 거의 안 보이므로 투명도를 올립니다 (globals.css 참고)
- **탈락한 카드에는 그림자를 주지 않습니다.** 가라앉아야 하니까요
- 그림자를 새로 만들지 마세요. 위 두 개가 전부입니다

---

## 5. 간격과 크기

4px 배수를 씁니다. **16px이 기본 리듬**입니다 (Starbucks의 `--space-3`).

- 페이지 최대 너비: **448px**. 한 손으로 쓰는 화면이라 넓히지 않습니다
- 페이지 좌우 여백: 20px
- 화면 안 블록 사이: 24px
- 카드 사이: 12px

### 터치 타깃 — 여기서만 원본을 크게 벗어납니다

Starbucks의 알약 버튼은 높이가 약 32px입니다. **원본 문서도 이게 접근성 기준에 못 미친다고
스스로 밝히고 있습니다.** 우리는 따르지 않습니다.

| 요소 | 최소 높이 |
|---|---|
| 주요 버튼 | 64px |
| 보조 버튼 | 56px |
| 메뉴 카드 | 96px |

**왜 이렇게 큰가**: 남의 폰을 급하게 누르는 상황이고,
한 번 잘못 누르면(= 남이 안 싫어하는 메뉴를 지우면) **되돌릴 방법이 없습니다.**

---

## 6. 레이아웃

### 주요 버튼은 항상 화면 아래쪽 같은 자리에

후보가 3개일 때와 6개일 때 버튼 위치가 달라지면 안 됩니다.
여러 명이 번갈아 누르는데 **버튼이 매번 다른 자리에 있으면 실수합니다.**

- 페이지는 `min-height: 100dvh`, 버튼 영역은 `mt-auto`로 바닥에 붙입니다
- `100vh`가 아니라 **`100dvh`**를 씁니다. 모바일 주소창 때문에 `vh`는 실제 화면보다 큽니다

### 결과 존은 화면 전체를 덮습니다

결과 화면의 `--house` 배경은 콘텐츠 폭(448px)이 아니라 **화면 전체 폭**을 덮습니다.
데스크톱에서 가운데만 색이 다르면 "구간"이 아니라 "박스"로 보입니다.

### 여백으로 나눕니다, 선으로 나누지 않습니다

구분선을 긋지 말고 여백과 면 색(`--surface`)으로 구역을 나눕니다.
크림 캔버스 자체가 흰 카드와 딥 밴드 사이의 숨 쉬는 공간입니다.

---

## 7. 컴포넌트 규칙

### 메뉴 카드 (`MenuCard`)

세로로 **이모지 → 이름 → 예시** 세 단 고정. 모서리 12px.

```
   🥟          ← 30px
   중식         ← 16px semibold
짜장면 · 탕수육   ← 11px --muted
```

| 상태 | 표현 |
|---|---|
| 기본 | `--card` 배경, `--elev-card` 그림자, 아주 옅은 테두리 |
| 눌림 | `scale(0.95)` |
| 탈락 | `--card-out-*` 색, **그림자 제거**, 이모지 흑백 + 투명도 40%, 이름에 취소선, 예시 자리에 "탈락" |
| 방금 탈락 | `.animate-knock-out` 한 번 |
| 룰렛 하이라이트 | `--brand-accent` 테두리 + 4px 링 + `scale(1.05)` + `--elev-lift` |

### 버튼

**모든 버튼은 알약형입니다. 예외 없습니다.** 눌림은 전부 `scale(0.95)`.

| 종류 | 배경 | 글자 | 높이 |
|---|---|---|---|
| 주요 (일반 면 위) | `--brand-accent` | `--brand-fg` | 64px |
| 주요 (결과 존 위) | `--brand-accent` | `--brand-fg` | 64px |
| 보조 (일반 면 위) | `--card` + 테두리 | `--foreground` | 56px |
| 보조 (결과 존 위) | 투명 + `--house-muted` 테두리 | `--house-fg` | 56px |
| 거부 | 보조와 같되 글자만 `--danger` / 결과 존 위에서는 `--danger-on-house` | | 56px |

> Starbucks는 진한 밴드 위에서 **흰 버튼 + 브랜드색 글자**로 뒤집습니다.
> 우리는 그렇게 하지 않고 결과 존 위에서도 주황을 채웁니다.
> **"이걸 누르면 된다"가 화면 어디서나 같은 색이어야** 하기 때문입니다.

- **한 화면에 주요 버튼은 하나만.** 두 개를 나란히 두면 급하게 누를 때 잘못 누릅니다
- 거부는 **정상적인 선택지이지 경고가 아닙니다.** 배경까지 빨갛게 칠하지 않습니다

### 전달 화면 (오버레이)

한 사람이 카드를 지운 뒤 덮이는 화면. `--background`를 95% 불투명도로 깔고 블러를 줍니다.

```
      🥟        ← 48px, 흑백
중식이 탈락했습니다   ← 24px bold
다음 사람(2번)에게    ← 15px --muted
 핸드폰을 넘겨주세요
   ( 넘겼어요 )     ← 알약형 주요 버튼, 화면 아래
```

- 조사는 `lib/korean.ts`의 `josa()`로 처리합니다 ("패스트푸드**가**" / "한식**이**")
- 마지막 사람이면 문구가 "결과를 볼까요?" / "남은 메뉴 보기"로 바뀝니다

### 포커스 (키보드 사용자)

모든 누를 수 있는 요소에 `--brand-accent` 색 2px 포커스 링.
`outline: none`만 주고 대체 표시를 안 넣는 것은 금지입니다.

---

## 8. 움직임 (이 서비스의 핵심)

**움직임은 "뭔가 일어났다"를 전달할 때만 씁니다. 분위기를 내려고 쓰지 않습니다.**

| 이름 | 길이 | 언제 | 무엇을 전달하나 |
|---|---|---|---|
| `scale(0.95)` | 0.2s | 버튼·카드를 누를 때 | "눌렸다" |
| `.animate-rise` | 0.28s | 화면이 바뀔 때 | "단계가 넘어갔다" |
| `.animate-knock-out` | 0.35s | 카드를 지울 때 | "이게 지워졌다" |
| 룰렛 하이라이트 | 약 2초 | 최종 결정할 때 | "무작위로 뽑히는 중이다" |
| `.animate-pop` | 0.45s | 결과가 나올 때 | "이걸로 정해졌다" |

### 룰렛의 감속

**등속으로 돌다 뚝 끊기면 "뽑혔다"가 아니라 "깜빡였다"로 보입니다.**
칸당 머무는 시간을 55ms에서 315ms까지 늘려 멈추는 느낌을 만듭니다.
(`ReadyScreen.tsx`의 `stepDelay`)

### 두 가지 제약

1. **동작 줄이기 설정을 존중합니다.** `prefers-reduced-motion: reduce`면
   CSS 애니메이션을 끄고, 룰렛도 건너뛰고 바로 결과로 갑니다
2. **연출에 쓴 시간은 KPI에서 뺍니다.** 이걸 안 하면 연출을 화려하게 만들수록
   "결정이 빠르다"는 우리 주장이 나빠 보입니다 (PROJECT_OVERVIEW.md 9절)

---

## 9. 다크 모드

`prefers-color-scheme`을 따릅니다. 토글 버튼은 만들지 않습니다(MVP 범위 밖).

**원본 Starbucks 문서에는 다크 모드가 없습니다.** 우리가 더한 부분이고, 원칙은 이렇습니다.

- 크림 캔버스는 **따뜻한 근검정**(`#171310`)으로 뒤집습니다. 중성 회색으로 가지 않습니다
- 주황은 **밝은 쪽으로** 뒤집습니다. 어두운 배경에서 진한 주황은 안 보입니다
- **그림자 투명도를 올립니다.** 어두운 면 위의 낮은 투명도 그림자는 보이지 않습니다
- **결과 존은 모드마다 방향이 반대입니다.** 라이트에서는 캔버스보다 더 따뜻하게,
  다크에서는 더 어둡게. 한쪽 값을 다른 쪽에 그대로 옮기면 흐름이 끊깁니다

`layout.tsx`의 `themeColor`도 라이트/다크 각각 지정해 주소창 색까지 맞춥니다.
**화면을 고친 뒤에는 라이트/다크 양쪽을 다 확인합니다.**

---

## 10. 반응형

기준점은 하나만 씁니다: `sm` (640px).

- **모바일이 기본입니다.** 메뉴 카드는 2열
- `sm` 이상: 제거 화면은 4열, 최종 선택 화면은 3열
- 데스크톱에서도 최대 448px에서 가운데 정렬. 넓히지 않습니다
- 단, 딥 밴드 배경은 화면 전체 폭

**항상 모바일에서 먼저 확인합니다.** 실기기 테스트가 가장 확실합니다.

---

## 11. 접근성 최소선

- 본문·보조 텍스트 대비 4.5:1 이상 (`--muted`와 `--house-muted`는 이 기준으로 정한 값)
- 탈락 카드는 색만으로 알리지 않습니다. **취소선 + "탈락" 글자 + `aria-label`**을 같이 씁니다
- 전달 화면 오버레이에는 `role="status"`를 붙여 화면 낭독기가 읽게 합니다
- 이모지에 의미를 싣지 않습니다. 이모지가 안 보여도 글자만으로 통해야 합니다

> **의도적인 예외**: `--card-out-fg`는 대비 약 3:1로 기준에 못 미칩니다.
> 탈락한 카드는 **눈에 안 띄는 것이 목적**이기 때문입니다.
> 대신 취소선·"탈락" 글자·`aria-label`로 정보를 중복해서 전달합니다.

---

## 12. 하지 말 것

- **AI 추천, 취향 학습, 개인화** — 이 서비스의 주장은 "AI 추천이 아니라 비선호 제거"입니다.
  디자인으로도 이걸 흐리지 마세요 (별점, "추천!" 배지, "당신을 위한" 문구 등)
- **최종 선택을 사람이 직접 하게 만들기** — 마지막에 협상이 다시 시작됩니다
- **순백 캔버스** — 크림의 온도가 이 디자인을 지탱합니다
- **각진 버튼** — 알약형은 예외 없습니다
- **그라데이션** — 단색 블록으로만 깊이를 만듭니다
- **진한 그림자 한 겹** — 항상 낮은 투명도로 2겹 쌓습니다
- 새 색·새 그림자 추가 — 필요하면 이 문서에 먼저 토큰을 추가하고 쓰세요
- 웹폰트·아이콘 라이브러리 설치 (필요하면 인라인 SVG 하나만)
- 화면 하나에서 두 가지를 동시에 묻기

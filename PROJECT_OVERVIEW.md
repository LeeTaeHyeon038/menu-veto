# 프로젝트 개요 — 바이브코딩을 활용한 MVP 서비스 만들기 (「우리 뭐 먹지?」)

> 이 문서는 새로운 대화 세션에서 프로젝트 맥락을 다시 설명하지 않아도 되도록 남겨두는 개요 문서입니다.
> 작업을 시작하기 전에 이 문서를 먼저 읽어 주세요.

## 1. 프로젝트 정보

- **과목**: 신인류 AI 사피엔스 경험디자인 (2026년 여름학기)
- **과제 유형**: 기말 프로젝트 (**조별과제**)
- **주제**: 바이브코딩(Vibe Coding)을 활용해 MVP 서비스를 직접 만들어 보기
- **작업 폴더**: 이 문서가 위치한 폴더 (`기말_프로젝트/menu-veto`) — 여기가 git 저장소 루트
- **진행 방식**: 여러 대화 세션에 걸쳐 Claude Code와 함께 단계별로 진행

### 이전 프로젝트 (`skku-notice-hub`)

같은 과제를 **성균관대 공지 통합 뷰어**라는 주제로 5단계까지 진행했으나,
조별과제 주제가 바뀌어 중단했습니다. 형제 폴더에 그대로 남아 있습니다.

```
기말_프로젝트/
├─ skku-notice-hub/   ← 이전 주제. 5단계까지 완료, 배포됨. 참고용
└─ menu-veto/         ← ★ 지금 이 프로젝트
```

**개요와 진행 방식(7단계)은 완전히 동일하고 서비스 주제만 다릅니다.**
막히면 저쪽 `PROJECT_OVERVIEW.md`와 코드를 열어 보세요. 12절에 무엇을 가져다 쓸 수 있는지 정리해 뒀습니다.

## 2. 목표

강의에서 제시된 7단계 흐름을 따라, 아이디어를 실제로 **동작하고 배포되어 사용 가능한 MVP 웹 서비스**로 완성하는 것.
완성도 높은 상용 제품이 아니라, 핵심 기능이 돌아가고 실제 사용자가 접근할 수 있는 최소 기능 제품(MVP)을 만드는 것이 목표입니다.

## 3. 진행 순서 (강의에서 제시된 7단계)

| 단계 | 이름 | 내용 | 상태 |
|:---:|---|---|:---:|
| 1 | 환경 설치 | CLI · IDE 세팅 | ✅ 완료 |
| 2 | 서비스 개발 | AI로 기능 구현 | ✅ 완료 |
| 3 | 디자인 | UI/UX 다듬기 | ✅ 완료 |
| 4 | 배포 | 웹에 올리기 | ✅ 완료 |
| 5 | DB 관리 | 데이터 저장·연동 | ✅ 완료 |
| 6 | 데이터 분석 | 사용 지표 분석 | ✅ 완료 |
| 7 | 검색 등록 | 구글 SEO 노출 | ✅ 완료 |

각 단계를 마칠 때마다 위 표의 상태를 업데이트합니다.

> **1단계는 이미 끝나 있습니다.** 이전 프로젝트에서 CLI·IDE·Node.js를 모두 설치했고
> 같은 컴퓨터에서 이어서 작업하므로 다시 할 것이 없습니다.
> (설치된 것: Node.js 24.19.0 LTS, Claude Code, VS Code, git)

## 4. 현재 상태

> **이 섹션은 세션이 끝날 때마다 갱신합니다.** 다음 세션은 여기부터 읽으면 됩니다.

- **마지막 업데이트**: 2026-08-16
- **현재 단계**: **7단계까지 전부 완료.** 남은 것은 데이터 수집과 발표 준비
- **서비스 주소**: https://menu-veto.vercel.app
- **저장소**: https://github.com/LeeTaeHyeon038/menu-veto (public)
- **지금까지 한 일**
  - 팀원이 제안한 기획안을 `기획안.md`로 정리 (원안 보존용)
  - 이 문서와 `CLAUDE.md` 작성, `.gitignore`·`.gitattributes` 추가
  - git 저장소 초기화 (기본 브랜치 `main`)
  - **GitHub 공개 저장소에 올림** → https://github.com/LeeTaeHyeon038/menu-veto
  - **5절의 미결정 4가지를 모두 확정** (기기 방식·서비스명·카테고리 수·제거 규칙 → 5절 표 참고)
  - **`web/`에 Next.js 16 + TypeScript + Tailwind v4 프로젝트 생성**
  - **화면 4개(시작 → 제거 → 최종 선택 → 결과)를 끝까지 통과되게 구현.**
    재선정 1회 제한, 걸린 시간 표시, 모바일 우선 레이아웃 포함
  - **KPI 기록 자리를 `web/src/lib/kpi.ts` 한 곳에 만들어 둠.**
    지금은 localStorage에만 쌓고, 5단계에서 이 파일 안에서 Supabase로 보내면 된다
  - **③ 최종 선택 화면에 룰렛 연출 추가** (11절 참고)
  - 핸드폰 실기기 테스트를 하며 두 가지 수정
    - `next.config.ts`에 `allowedDevOrigins` 추가 — 개발 서버가 사설 IP의 JS 요청을
      막아서 핸드폰에서는 버튼이 하나도 안 눌렸다
    - `crypto.randomUUID`는 https·localhost에서만 동작한다. 핸드폰 http 접속에서
      터지지 않도록 대체 ID를 두었다
  - **`DESIGN.md` 작성** — 강의에서 안내한 [awesome-design-md](https://github.com/voltagent/awesome-design-md)의
    **Starbucks 디자인 시스템을 바탕으로** 우리 서비스에 맞게 다시 썼다.
    고른 이유와 무엇을 바꿨는지는 DESIGN.md 맨 위 "출처"에 정리돼 있다
    - 후보 5개(Airbnb·Starbucks·Spotify·Uber·Pinterest)를 받아 비교했고,
      Airbnb·Pinterest는 **사진이 시스템의 핵심**이라 사진이 없는 우리에겐 맞지 않았다
    - **이전 프로젝트 문서는 가져오지 않았다.** 저쪽은 "훑는" 도구라
      애니메이션을 금지했지만 여기서는 움직임이 기능이기 때문 (DESIGN.md 0절)
  - 그 규칙대로 화면을 다시 칠함
    - 크림 캔버스 3단계(캔버스 → 세라믹 → 흰 카드), 주황 4단계
    - **모든 버튼을 알약형으로**, 카드는 12px 모서리 + 2겹 그림자
    - **결과 화면 전체를 딥 밴드(진한 갈색)로** — 이 서비스의 클라이맥스라서
    - 터치 타깃 확대(카드 96px·주요 버튼 64px), 주요 버튼을 화면 하단 고정
    - **결과 존은 모드마다 방향이 반대다.** 처음엔 라이트 모드에서도 어둡게 깔았는데
      "앞 화면들과 테마가 안 맞는다"는 지적이 나와, 라이트는 살구빛으로 뒤집었다.
      *특별한 구간의 신호는 "면 색이 바뀐다"이지 "어둡다"가 아니다* (DESIGN.md 1절)
  - **`README.md` 작성** — 저장소를 공개할 것이므로 서비스 소개·실행법·구조를 정리
  - **Supabase 연결 완료 (5단계)** — 브라우저 → `events` 테이블 → 집계 뷰까지 동작 확인
    - 스키마·정책·뷰는 `web/supabase/schema.sql` 하나에 다 들어 있다
    - **권한을 두 겹으로 잠갔다.** GRANT는 insert만 주고, RLS도 insert 정책만 둔다.
      읽기를 시도하면 `permission denied`로 막힌다 (실제로 확인함)
    - 집계 뷰에는 `security_invoker`를 켜서 뷰로 우회하는 길도 막았다
    - 배포본 JS에 anon 키만 박히고 `service_role` 흔적이 없는 것도 확인했다
    - 첫 실측: 세션 1건, 완료율 100%, 평균 결정시간 6.3초.
      **혼자 빠르게 눌러 나온 값이라 실사용과 다르다.** 숫자보다 "측정이 된다"가 확인된 것
  - **GA4 연결 완료 (6단계)** — 측정 ID `G-9CL7ZLG4P4`, 실시간 보고서에서 확인함
    - `@next/third-parties`의 `GoogleAnalytics` 컴포넌트를 쓴다.
      GA 화면이 안내하는 스크립트를 직접 붙여넣으면 **태그가 두 번 들어가 중복 집계된다**
    - **이 앱은 화면이 바뀌어도 주소가 그대로**라, GA4가 자동으로 재는 페이지 이동이
      처음 한 번밖에 안 잡힌다. 그래서 단계 전환을 직접 이벤트로 보낸다
    - **이벤트 이름에 `menu_` 접두사를 붙인다.** GA4에는 예약된 이름이 있고
      `session_start`가 거기 포함된다. 실시간 보고서에 `menu_session_start`와
      `session_start`가 따로 잡히는 것으로 확인됨
    - `NEXT_PUBLIC_GA_ID`는 **Vercel Production에만** 넣는다.
      로컬·Preview에서 누른 것까지 방문자로 잡히면 지표가 오염된다
  - **검색 등록·링크 미리보기 완료 (7단계)**
    - `sitemap.xml`·`robots.txt`를 Next.js 파일 규약(`app/sitemap.ts`, `app/robots.ts`)으로 생성
    - Google Search Console 소유권 확인 + 사이트맵 제출 (상태: 성공, 발견된 페이지 1)
    - **카톡 링크 미리보기 이미지**를 `app/opengraph-image.tsx`로 만든다
      - ⚠️ 이미지 생성기 기본 폰트에 **한글 글리프가 없다.** 폰트를 직접 넘겨야 한다
      - `web/assets/og-font.ttf`는 Noto Sans KR을 **이 이미지에 쓰는 글자만 남긴** 4.5KB 파일이다.
        **문구를 바꾸면 폰트도 다시 받아야 한다.** 없는 글자는 빈칸으로 나온다
      - Google Fonts는 요청 UA에 따라 형식을 달리 준다. IE6 UA면 EOT가 와서 빌드가 깨진다.
        안드로이드4 UA로 받아야 TTF가 온다
    - **검색 노출은 기대하지 말 것.** 페이지 하나짜리 신생 사이트다.
      발표에서는 "등록 절차를 완료하고 색인을 요청했다"까지가 정확한 표현.
      이 서비스에서 실제로 값을 하는 건 **카톡 미리보기** 쪽이다
  - **Vercel 배포 완료** → https://menu-veto.vercel.app
    - 새 저장소는 Vercel의 GitHub 앱 권한이 "선택한 저장소만"이라 목록에 안 뜬다.
      Import 화면의 `Adjust GitHub App Permissions`에서 저장소를 추가해야 보인다
    - Root Directory를 `web`으로 지정했다
  - `npm run lint` / `npm run build` 통과 확인
- **바로 다음에 할 일 (다음 세션은 여기서 시작)**

  **개발은 끝났다. 남은 것은 데이터와 발표다.**

  1. **지금 당장 링크를 돌릴 것.** 쓰는 만큼 KPI가 쌓인다.
     GA4 일반 보고서는 반영에 24~48시간 걸리므로 **발표 최소 이틀 전에는 시작**해야 한다
  2. 본격 측정 전에 테스트 기록을 한 번 비울지 정할 것
     (`delete from public.events;`)
  3. 팀원 실기기 점검 — 라이트/다크 양쪽, 차례 표시가 헷갈리지 않는지,
     **재선정 1회 제한이 답답하지 않은지** (이건 서비스 핵심 주장이라 반응이 발표 거리가 된다)
  4. 발표 준비 — **9절의 "말할 수 있는 것 / 없는 것" 표를 반드시 먼저 읽을 것**

### KPI 보는 법

Supabase 대시보드 > SQL Editor 에서:

```sql
select * from public.kpi_summary;      -- KPI 4개
select * from public.kpi_veto_ranking; -- 어떤 카테고리가 많이 탈락하나
```

읽기는 공개 키로 막혀 있으므로 **대시보드에서만** 볼 수 있다.
실제 측정을 시작하기 전에 테스트로 남은 기록은 지울 것:

```sql
delete from public.events where session_id like 'smoke-test-%';
```

### GA4 보는 법

https://analytics.google.com > 속성 「우리 뭐 먹지?」 > 보고서

- **실시간**: 지금 접속 중인 사람. 설정이 잘 됐는지 확인할 때 쓴다
- **일반 보고서**: 방문자 수·유입 경로·이탈. **데이터 반영에 24~48시간 걸린다.**
  발표용 숫자가 필요하면 최소 하루 전에는 쌓아둘 것

우리가 보내는 이벤트는 전부 `menu_`로 시작한다.
`menu_session_start`가 몇 명이 시작했는지, `menu_accepted`가 몇 명이 끝냈는지다.

> **Supabase와 GA4는 역할이 다르다.**
> Supabase = 서비스 "안에서" 무엇을 했나 (결정 시간, 수용률)
> GA4 = 서비스 "밖에서" 어떻게 왔고 어디서 나갔나 (유입, 이탈 지점)
>
> 광고 차단기가 켜져 있으면 GA4는 막히지만 Supabase 기록은 그대로 들어간다.
> 두 곳에 나눠 둔 것이 여기서 값을 한다.
- **막힌 것**: 없음

## 5. 서비스 아이템

**「우리 뭐 먹지?」** — 여러 명이 같이 먹을 메뉴를 **각자 싫은 것부터 지워서** 빠르게 정하는 웹서비스.

상세 기획은 **`기획안.md`**(팀원 제안 원안)에 있습니다. 요약하면:

- **문제**: 여러 명이 메뉴를 정할 때 "제안 → 거절"이 반복되며 시간이 오래 걸린다
- **핵심 뒤집기**: "뭐 먹고 싶어?"가 아니라 **"오늘 절대 먹기 싫은 건 뭐야?"**를 묻는다
- **흐름**: 인원 선택 → 한 사람씩 싫은 카테고리 1개 제거 → 남은 후보 중 랜덤 확정
  → 수용 확인 → 거부 시 **딱 한 번만** 재선정
- **후보**: 개별 음식명(짜장면·마라탕…)이 아니라 **음식 카테고리 8개**
  (한식 / 중식 / 일식 / 양식 / 패스트푸드 / 분식 / 가벼운 식사 / 국밥·찌개류)
- **핵심 가설**: 전체 랜덤 추천보다, 비선호를 먼저 제거한 결과가 **더 잘 받아들여질 것이다**

### ✅ 확정된 규칙 (2026-08-16 결정)

기획안에 명시되지 않았던 항목들을 2단계 시작 전에 결정했습니다.

| # | 항목 | 결정 | 근거 |
|:---:|---|---|---|
| ① | **기기 방식** | **한 기기를 돌려가며 사용** | (b) 각자 기기 방식은 방 만들기·서버·실시간 동기화가 전부 필요해 MVP 범위를 넘어선다. (a)는 브라우저 하나에서 상태만 관리하면 되고, 기획안의 "핸드폰 전달" 원안과도 같다 |
| ② | **서비스명** | **「우리 뭐 먹지?」** | 문제 상황을 그대로 드러내 직관적. 폴더·저장소 이름(`menu-veto`)과 다른 것은 상관없다 |
| ③ | **카테고리 수** | **8개 그대로** (한식/중식/일식/양식/패스트푸드/분식/가벼운 식사/국밥·찌개류) | 기획안 원안 유지 |
| ④ | **제거 규칙** | **1인 1개 고정, 인원은 2~5명** | 최악(5명)에도 후보 3개가 남아 랜덤 선정이 의미를 유지한다. 인원별로 규칙을 바꾸면 복잡도만 늘고 MVP에 이득이 없다 |

> ④의 부수 결정: 기존 "5명+" 선택지는 **5명으로 상한**을 둔다.
> 6명 이상을 받으면 후보가 2개 이하로 줄어 "선택"이 무의미해지기 때문이다.
> 6명 이상 상황은 필요해지면 나중에 검토한다 (6절 "나중에 검토할 것").

## 6. MVP 기능 범위

기획안 13절을 기준으로, 넣을 것과 뺄 것을 정리했습니다.

**넣는 것**

1. 시작 화면 — 인원 선택 (2 / 3 / 4 / 5명)
2. 메뉴 카드 8개 표시
3. 한 사람씩 차례로 카드 1개 제거 (**지금 몇 번째 사람 차례인지 표시**)
4. 제거 애니메이션 + "○○이 탈락했습니다" 피드백
5. 남은 후보 중 랜덤 최종 선정
6. 결과 화면 — 선택된 메뉴 + **걸린 시간 표시**
7. 결과 수용 / 거부 버튼
8. 재선정 **최대 1회** 제한
9. 모바일 화면 대응 (**주 사용 환경이 모바일이다. 데스크톱보다 우선**)

**빼는 것 (의도적으로 제외)**

- 로그인·회원가입
- 각자 기기에서 실시간 참여 (5절 ① 참고)
- AI 추천 — 이 과제는 *AI로 서비스를 만드는 것*이지 *서비스가 AI를 쓰는 것*이 아님.
  **게다가 이 서비스의 핵심 주장이 "AI 추천이 아니라 비선호 제거"다**
- 주변 식당 검색·지도 연동
- 결과 공유 링크

**나중에 여유가 되면 검토할 것** (기획안 19절)

- 식사 시간대 선택 (점심 / 저녁)
- 식사 강도 선택 (헤비하게 / 가볍게 / 상관없음)
- 카테고리 → 구체적 음식 추천 한 단계 더

## 7. 기술 스택과 각 도구의 역할

| 항목 | 선택 | 역할 |
|---|---|---|
| 프레임워크 | **Next.js + TypeScript** | 화면을 만드는 도구. 이전 프로젝트에서 쓴 것과 같아 세팅을 그대로 재활용할 수 있다 |
| 코드 저장 | **GitHub** | 코드 보관 + 버전 관리. 되돌리기 가능 |
| 배포 | **Vercel** | GitHub 코드를 실제 웹사이트로 띄움. GitHub에 올리면 자동 반영 |
| DB | **Supabase** | **KPI 측정 기록**을 저장 (무료 Postgres). 5단계에서 도입 |
| 분석 | **Google Analytics 4** | 방문자 수·이탈 지점 측정 (6단계) |
| 디자인 | **DESIGN.md** | 디자인 규칙을 정리한 마크다운. AI에게 주면 일관된 UI가 나옴. **작성 완료** |

> **기획안 13절은 "DB 없이 HTML/CSS/JS로 가능"이라고 했고, 그 말이 맞습니다.**
> 서비스 자체는 브라우저 안에서만 돌아도 완성됩니다.
> 그런데도 Next.js와 Supabase를 쓰는 이유는 **과제가 7단계를 요구하기 때문**이고,
> 더 중요하게는 **16절의 KPI 4개를 측정하려면 기록을 남길 곳이 필요하기 때문**입니다. (9절 참고)

## 8. 개발 순서

**화면 먼저, 기록·분석은 나중.** 가짜 데이터 없이도 전체 흐름(인원 선택 → 제거 → 결과)이
바로 눈에 보이는 서비스이므로, 2단계에서 **동작하는 화면부터 끝까지 만들고** 그 뒤에 다듬습니다.

한 화면씩 완성하기보다 **처음부터 끝까지 한 번 통과되게** 만든 뒤 되돌아와 다듬는 편이 좋습니다.
이 서비스는 "흐름 자체가 상품"이라 중간만 보면 판단이 안 되기 때문입니다.

## 9. 핵심 가설과 KPI (이 프로젝트의 특이점)

이전 프로젝트와 가장 크게 다른 점입니다. **이 서비스는 검증할 가설이 있습니다.**

> 전체 메뉴 중 랜덤으로 추천하는 것보다, 각자의 비선호를 먼저 제거하면
> 최종 결과를 받아들이는 비율이 높아질 것이다.

이 가설을 확인하려면 **사용 기록을 남겨야 합니다.**

| # | KPI | 측정 방법 |
|:---:|---|---|
| ① | 평균 메뉴 결정 시간 | 시작 클릭 ~ 최종 수용 클릭까지 자동 측정 |
| ② | 첫 결과 수용률 | 첫 결과를 바로 받아들인 비율 |
| ③ | 재선정률 | 첫 결과를 거부한 비율 |
| ④ | 최종 결정 완료율 | 시작한 그룹 중 끝까지 마친 비율 |

**그래서 5단계(DB)와 6단계(분석)에 자연스러운 목적이 생깁니다.**
이전 프로젝트에서는 DB가 "공지를 저장할 곳"이었지만, 여기서는
**"가설을 검증할 근거를 모으는 곳"**입니다. 발표에서 이야기할 거리도 여기서 나옵니다.

→ **2단계에서 화면을 만들 때부터 "언제·무엇을 기록할지"를 염두에 둘 것.**
   나중에 붙이기보다 처음부터 자리를 만들어 두는 편이 훨씬 쉽습니다.
   기록은 전부 `web/src/lib/kpi.ts` 한 파일에 모아 뒀습니다.

### 측정할 때 지킬 것 (2단계에서 실제로 걸렸던 문제)

- **기록이 실패해도 서비스는 계속 돌아가야 한다.** KPI는 부수 기능이다.
  12절의 "환경변수가 없으면 바로 멈추게 만들 것"과 반대로 보이지만 기준이 다르다.
  *데이터가 비면 서비스가 거짓말을 하는 경우엔 멈추고, 기록만 안 남는 경우엔 계속 간다.*
- **연출에 쓴 시간은 결정 시간에서 뺀다.** 룰렛 애니메이션 2초를 그대로 두면
  연출을 화려하게 만들수록 "결정이 빠르다"는 우리 주장이 나빠 보인다.
  사용자가 고민한 시간이 아니기 때문이다 (`page.tsx`의 `overheadMsRef`).

### ⚠️ 이번 학기에는 가설을 검증하지 않습니다 (2026-08-16 결정)

**결론: 효과를 검증했다고 말하지 않고, "향후 KPI 검증 계획"으로 발표합니다.**
DB와 측정 체계는 그대로 만들되, 발표에서는 **기술적 수치만** 이야기합니다.

**이유 두 가지. 두 번째가 더 결정적입니다.**

**① 표본이 부족하다**

② 첫 결과 수용률을 통계적으로 주장하려면 (유의수준 5%, 검정력 80% 기준):

| 보려는 차이 | 필요한 세션 수 |
|---|---|
| 50% → 80% (큰 차이) | 그룹당 약 19회, 총 38회 |
| 50% → 65% (중간 차이) | 그룹당 약 84회, 총 168회 |

팀원 5명이 써서는 10~20회가 한계입니다.

**② 비교할 대상이 아예 없다 — 표본을 늘려도 해결되지 않는다**

가설은 *"**전체 랜덤 추천보다** 비선호를 먼저 제거한 결과가 더 잘 받아들여진다"*는
**비교 문장**입니다. 그런데 앱에는 "비선호 제거" 흐름 하나뿐이라
전체 랜덤 추천을 경험하는 사용자가 **아무도 없습니다.**

즉 데이터를 아무리 모아도 *"우리 방식의 수용률은 78%였다"*까지만 말할 수 있고,
*"랜덤보다 높다"*는 말은 못 합니다. 비교군의 숫자가 존재하지 않기 때문입니다.

> 검증하려면 앱 안에 A/B 비교군(무작위로 절반은 제거 없이 바로 랜덤 추천)을
> 넣어야 합니다. 이번에는 넣지 않기로 했습니다. **나중에 이어서 한다면 여기서 시작하세요.**

### 그래서 발표에서 말할 수 있는 것 / 없는 것

| ✅ 말할 수 있다 | ❌ 말하면 안 된다 |
|---|---|
| "결정에 평균 N초가 걸렸다" | "랜덤보다 빠르다" |
| "첫 결과 수용률이 N%였다" | "비선호 제거가 수용률을 높였다" |
| "측정 체계를 갖췄고 검증 계획은 이렇다" | "가설이 검증되었다" |

팀원 5명이 직접 써보는 것은 **효과 검증이 아니라 기능·사용성 점검**입니다.
발표에서 이 둘을 반드시 구분해 표현할 것.

## 10. V1 → V2 → V3 (발표용 개선 서사)

기획안 18절에 정리된 내용입니다. **이미 V2까지 결론이 나 있으므로 V2부터 만듭니다.**

| 버전 | 내용 | 상태 |
|---|---|---|
| V1 | 개별 음식명으로 후보 구성 (짜장면·마라탕…) | 기획 단계에서 문제 발견 → 폐기 |
| **V2** | **음식 카테고리 8개로 단순화** | ★ **여기서 시작** |
| V3 | 재선정 최대 1회 제한 | MVP에 포함 |

> V1을 실제로 만들 필요는 없습니다. "왜 카테고리로 바꿨는가"라는 **의사결정 과정 자체**가
> 발표 자료의 내용이 됩니다. 이 판단 근거는 `기획안.md` 6절과 18절에 남아 있습니다.

## 11. 화면 구성

기획안 14절 참고. 네 화면이면 끝납니다.

```
① 시작        "우리 뭐 먹지?" + 인원 선택 (2/3/4/5명)
② 제거        "N/M 번째 사람" + 카드 8개 + "절대 먹기 싫은 것 하나"
③ 최종 선택   "모두가 먹을 수 있는 메뉴만 남았습니다" + [최종 결정하기]
④ 결과        선택된 메뉴 + 걸린 시간 + [좋아, 먹으러 가자] / [이건 진짜 못 먹겠어]
```

**②에 "다음 사람에게 넘겨주세요" 화면을 한 단계 끼웠습니다.**
한 기기를 돌려쓰는 방식이라 "내 차례가 끝났다"는 신호가 없으면 한 사람이 두 개를 지웁니다.

**③에서 카드는 직접 고를 수 없고, 룰렛 연출로 무작위 확정합니다.**
처음에는 카드가 눌리지 않아 고장난 것처럼 보인다는 지적이 있었는데,
직접 고르게 하면 마지막에 "이거 어때? → 난 별로" 협상이 다시 시작됩니다.
그래서 **고르는 기능을 주는 대신, 뽑히는 과정을 보여주는 쪽**으로 해결했습니다.
카드가 순서대로 하이라이트되며 점점 느려지다 당첨 카드에서 멈춥니다.
같은 이유로 화면에 "왜 직접 못 고르는지" 안내 문구를 넣었습니다.

## 12. 이전 프로젝트에서 가져올 것

`../skku-notice-hub/` 에서 그대로 베끼거나 참고할 수 있는 것들입니다.
**바퀴를 다시 발명하지 마세요.** 저쪽에서 이미 겪은 시행착오가 많습니다.

| 가져올 것 | 위치 | 비고 |
|---|---|---|
| Next.js 프로젝트 세팅 | `web/` 전체 구조 | TypeScript + Tailwind v4 + App Router 조합 |
| 디자인 토큰 체계 | `web/src/app/globals.css`, `DESIGN.md` | **색은 CSS 변수로만 쓰는 방식**. 라이트/다크 자동 대응 |
| Supabase 연결 | `web/src/lib/supabase.ts`, `web/supabase/schema.sql` | RLS 정책 예시 포함 |
| 환경변수 분리 방식 | `web/.env.example` | 어디에 공개 키를 두고 어디에 비밀 키를 두는지 |
| `.gitignore` / `.gitattributes` | 저장소 루트 | 이미 이 폴더에 복사해 뒀음 |

**저쪽에서 배운 교훈 (반복하지 말 것)**

- **Vercel의 Root Directory를 `web`으로 지정**할 것. 앱이 저장소 루트가 아니라
  `web/` 안에 있어서, 기본값(루트)으로 두면 `package.json`을 못 찾아 빌드가 실패한다

- Vercel 환경변수는 **새 배포에만 적용**된다. 값을 넣은 뒤 반드시 재배포할 것
- 환경변수는 값이 비어 있어도 저장된다. 넣은 뒤 **눈 아이콘으로 값을 확인**할 것
- 저장소가 public이면 **커밋 전에 비밀값이 섞였는지 확인**할 것
- 환경변수가 없으면 조용히 넘어가지 말고 **바로 멈추게** 만들 것.
  그래야 "데이터가 빈 멀쩡한 사이트"가 배포되는 사고를 막는다

## 13. 참고 링크

- DESIGN.md 모음 (강의 권장): https://github.com/voltagent/awesome-design-md
  → **여기 있는 Starbucks 문서를 바탕으로 우리 `DESIGN.md`를 썼다.**
  73개가 있고, 저장소의 `design-md/<이름>/DESIGN.md` 경로에서 원문을 받을 수 있다
- DESIGN.md 생성 사이트 (강의 권장): https://getdesign.md/
  → 웹페이지에는 요약만 보이고 전문은 안 나온다. `npx getdesign@latest add <이름>`
  또는 위 저장소에서 직접 받는 편이 빠르다
- Google Analytics 등록 (강의 권장): https://analytics.google.com/analytics/web/#/provision

## 14. 문서 관리 규칙

- 이 문서 하나로 개요와 진행 상황을 함께 관리합니다. (4절이 인수인계 역할)
- **`기획안.md`는 팀원 제안 원안이므로 고치지 않습니다.** 논의로 바뀐 내용은 이 문서에 씁니다.
- 진행 로그가 길어져 현재 상태를 한눈에 보기 어려워지면 `PROGRESS.md`로 분리합니다.
- 작업 규칙은 `CLAUDE.md`에 별도로 정리되어 있습니다.

# 팀원 인수인계 — 「우리 뭐 먹지?」

> **발표자료를 만드는 팀원이 읽는 문서입니다.**
> 개발은 끝났습니다. 여기에 링크·숫자 보는 법·발표에서 쓸 이야깃거리를 정리했습니다.
> 더 자세한 내용은 [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md)에 있습니다.

## 링크

| | 주소 |
|---|---|
| **서비스** | https://menu-veto.vercel.app |
| **코드** | https://github.com/LeeTaeHyeon038/menu-veto |
| 방문 지표 | Google Analytics 4 (계정 초대 필요) |
| 사용 기록 | Supabase — 원본은 `events` 테이블 (아래 "원본 데이터" 참고) |

서비스는 **핸드폰으로** 열어야 제대로 보입니다. 모바일이 주 사용 환경입니다.

## 이 서비스가 뭔가

여러 명이 **핸드폰 하나를 돌려가며** 각자 "절대 먹기 싫은" 메뉴를 하나씩 지워
메뉴를 정하는 서비스입니다.

- ❌ "뭐 먹고 싶어?" → 제안과 거절이 반복되며 안 끝난다
- ✅ **"오늘 절대 먹기 싫은 건?"** → 후보가 줄어들고 반드시 끝난다

흐름: **인원 선택(2~5명) → 한 명씩 카테고리 1개 제거 → 남은 후보 중 룰렛으로 확정 → 수용/거부(1회만)**

---

## ⚠️ 발표에서 말할 수 있는 것 / 없는 것

**이 절을 먼저 읽어 주세요.** 잘못 말하면 사실이 아닌 발표가 됩니다.

| ✅ 말할 수 있다 | ❌ 말하면 안 된다 |
|---|---|
| "결정에 평균 N초가 걸렸다" | "랜덤 추천보다 빠르다" |
| "첫 결과 수용률이 N%였다" | "비선호 제거가 수용률을 높였다" |
| "측정 체계를 갖췄고 검증 계획은 이렇다" | "가설이 검증되었다" |

**왜 그런가**: 원래 가설은 *"**전체 랜덤 추천보다** 비선호를 먼저 제거한 결과가
더 잘 받아들여진다"*는 **비교 문장**입니다. 그런데 앱에는 "비선호 제거" 흐름 하나뿐이라
**전체 랜덤 추천을 경험한 사용자가 아무도 없습니다.**

즉 비교 대상의 숫자가 존재하지 않습니다. 데이터를 아무리 모아도
*"우리 방식은 N%였다"*까지만 말할 수 있습니다.

> **이건 약점이 아니라 발표 소재입니다.**
> "왜 검증이 불가능한지, 검증하려면 무엇이 필요한지(A/B 비교군)를 정확히 파악했다"는
> 것 자체가 보여줄 내용입니다. 어설프게 "검증했다"고 말하는 것보다 훨씬 낫습니다.

또한 팀원이 직접 써 본 것은 **효과 검증이 아니라 기능·사용성 점검**입니다.
발표에서 이 둘을 구분해 주세요.

---

## 숫자 보는 법

### KPI 4개 — Supabase

대시보드 → SQL Editor에 붙여넣고 Run. 결과 오른쪽 위 **Export → CSV**로 받을 수 있습니다.

```sql
select * from public.kpi_summary;
```

| 나오는 값 | 뜻 |
|---|---|
| 시작한_세션 / 완료한_세션 | 몇 번 시작했고 몇 번 끝냈나 |
| 완료율_퍼센트 | **KPI ④** |
| 평균_결정시간_초 | **KPI ①** (룰렛 연출 시간은 이미 뺀 값) |
| 첫결과_수용률_퍼센트 | **KPI ②** |
| 재선정률_퍼센트 | **KPI ③** |

**덤으로 이것도 있습니다.** 발표에서 반응이 좋을 자료입니다.

```sql
select * from public.kpi_veto_ranking;   -- 어떤 카테고리가 가장 많이 탈락했나
```

### 방문자·이탈 — Google Analytics 4

보고서 → 실시간(즉시) / 일반 보고서(**반영에 24~48시간**).
우리가 보낸 이벤트는 전부 `menu_`로 시작합니다.

- `menu_session_start` — 시작한 사람
- `menu_veto` — 카드를 지운 횟수
- `menu_accepted` — 끝까지 마친 사람

> ⏰ **발표 최소 이틀 전에는 링크를 돌려 데이터를 쌓아야 합니다.**
> 지금 뽑으면 거의 비어 있습니다.

---

## 발표에 쓸 만한 이야깃거리

만들면서 실제로 부딪힌 판단들입니다. 근거가 문서와 커밋 메시지에 남아 있습니다.

**① 최종 선택을 사람이 직접 못 하게 했다**
테스트 중 "카드가 안 눌린다, 선택된 걸 모르겠다"는 지적이 나왔습니다.
그런데 직접 고르게 하면 마지막에 *"이거 어때? → 난 별로"* 협상이 다시 시작됩니다.
서비스가 없애려던 바로 그 상황이죠. 그래서 **고르는 기능을 주는 대신
뽑히는 과정을 보여주는** 룰렛 연출로 해결했습니다.

**② 개별 음식명 → 카테고리 8개 (V1 → V2)**
짜장면·마라탕 같은 개별 음식으로 하면 후보가 너무 많아 "지우기"가 끝나지 않습니다.
**V1을 실제로 만들 필요는 없었고, 이 판단 과정 자체가 발표 내용입니다.**

**③ 이전 프로젝트의 디자인 규칙을 안 가져왔다**
지난 학기 공지 뷰어는 "훑는" 도구라 *애니메이션 금지*가 옳았습니다.
여기는 정반대로 **움직임이 기능**입니다. 룰렛도 탈락 애니메이션도
"뭔가 일어났다"를 전달하는 유일한 수단이었습니다.
디자인 규칙은 [awesome-design-md](https://github.com/voltagent/awesome-design-md)의
**Starbucks** 디자인 시스템을 바탕으로 다시 썼습니다 ([DESIGN.md](DESIGN.md) 맨 위 참고).

**④ 연출에 쓴 시간을 KPI에서 뺐다**
룰렛 애니메이션 약 2초를 그대로 두면, **연출을 화려하게 만들수록
"우리가 빠르다"는 주장이 나빠 보입니다.** 사용자가 고민한 시간이 아니니까요.

**⑤ 결과 화면 색은 모드마다 방향이 반대다**
처음엔 라이트 모드에서도 결과 화면을 어둡게 깔았는데
"앞 화면들과 테마가 안 맞는다"는 지적이 나왔습니다.
특별한 구간의 신호는 **"면 색이 바뀐다"이지 "어둡다"가 아니었습니다.**

**⑥ 기록이 실패해도 서비스는 계속 돈다**
KPI 저장이 안 된다고 메뉴를 못 정하게 만들면 안 됩니다.
실제로 개발 중 이것 때문에 앱이 멈춘 적이 있어서 고쳤습니다.

---

## 원본 데이터 (`events` 테이블)

**이 표 하나가 원본입니다.** 위의 KPI 뷰 세 개는 전부 이 표에서 계산해 낸 것이라,
직접 분석하고 싶다면 이것만 있으면 됩니다.

```sql
select * from public.events order by created_at;
```

### 한 줄 = 한 사건

사용자가 무언가 할 때마다 한 줄씩 쌓입니다. 한 번의 메뉴 결정(= 한 세션)은
보통 여러 줄로 이루어집니다.

| 열 | 뜻 |
|---|---|
| `session_id` | **한 번의 메뉴 결정을 묶는 키.** 같은 값끼리 한 세션 |
| `type` | 무슨 일이 일어났나 (아래 표) |
| `people_count` | 인원 수 (2~5) |
| `turn` | 몇 번째 사람 차례였나 |
| `menu_id` | 어떤 카테고리인가 |
| `attempt` | 몇 번째 결과인가 (1 = 첫 결과, 2 = 재선정 결과) |
| `elapsed_ms` | 시작부터 걸린 시간(밀리초). **룰렛 연출 시간은 이미 빼고 기록됩니다** |
| `created_at` | 시각 |

**이벤트 종류마다 채워지는 칸이 다릅니다.** 나머지는 비어 있는 게 정상입니다.

| `type` | 언제 | 채워지는 칸 |
|---|---|---|
| `session_start` | 인원을 골라 시작 | `people_count` |
| `veto` | 한 사람이 카테고리 하나를 지움 | `turn`, `menu_id` |
| `result_shown` | 결과 화면이 보임 | `menu_id`, `attempt`, `elapsed_ms` |
| `accepted` | "좋아, 먹으러 가자" | `menu_id`, `attempt`, `elapsed_ms` |
| `rejected` | "이건 진짜 못 먹겠어" | `menu_id`, `attempt` |
| `restart` | 처음부터 다시 | — |

`menu_id` 값: `korean`(한식) `chinese`(중식) `japanese`(일식) `western`(양식)
`fastfood`(패스트푸드) `bunsik`(분식) `light`(가벼운 식사) `soup`(국밥·찌개류)

### 직접 계산할 때 주의할 점

- **반드시 `session_id`로 묶어서 계산해야 합니다.** 줄 단위로 세면 의미가 없습니다
- 완료한 세션 = 그 `session_id`에 `accepted`가 있는 세션
- **시작만 하고 끝내지 않은 세션도 그대로 남아 있습니다.** 완료율을 재려면 필요한 데이터라
  일부러 지우지 않았습니다. 빼고 계산하면 완료율이 항상 100%가 됩니다
- 결정 시간은 `accepted` 줄의 `elapsed_ms`입니다 (1000으로 나누면 초)

> `events`는 열 이름과 값이 전부 영문이라 **엑셀에서 한글 깨짐 문제가 없습니다.**
> (한글 열 이름을 쓰는 KPI 뷰를 내보낼 때만 UTF-8 문제가 생깁니다)

---

## 더 읽을 곳

| 파일 | 무엇이 있나 |
|---|---|
| [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) | 전체 개요·진행 기록·결정 근거. **9절이 위 경고의 원문** |
| [DESIGN.md](DESIGN.md) | 색·글자·움직임 규칙과 그 이유 |
| [기획안.md](기획안.md) | 팀원이 낸 최초 기획안 (원안 보존용) |
| `git log` | 각 결정의 이유를 커밋 메시지에 남겨 뒀습니다 |
